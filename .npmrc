strict-peer-dependencies=true
shared-workspace-lockfile = true
# Pin the public registry so installs aren't hijacked by a user-level
# ~/.npmrc pointing at a private registry from another project.
registry=https://registry.npmjs.org/
