'use client';

// The post-call outcome/notes view is part of the shared dialer UI. Re-exported
// here so existing `./post-call.view` imports keep working unchanged.
export { PostCallView } from '@ringee/dialer-ui';
