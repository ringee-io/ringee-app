/**
 * AI Assistant module metadata. The module ships as a Beta surface; the
 * version is shown in the header launcher tooltip so testers can report
 * against a concrete build.
 */
export const AI_MODULE_VERSION = '0.1.0';
